import styled from 'styled-components';

const Card = styled.div`
    display: flex;
    padding: 50px 10%;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
`;

export default Card;
